function Footer() {
  return (
    <footer className="footer">
      <p>
        Technologies concepts React used: React, Typescript (TSX), Context API
        props, useState, useEffect, component composition, children prop,
        conditional rendering, array methods (map, filter), event handling.
      </p>
    </footer>
  );
}

export default Footer;
